---
apiVersion: storage.k8s.io/v1
# https://kubernetes.io/docs/concepts/storage/storage-classes/
kind: StorageClass
metadata:
  name: managed-nfs-storage
  namespace: "{{CSI_NFS_NAMESPACE}}"
provisioner: "{{CSI_NFS_PROVISIONER_NAME}}"
parameters:
  archiveOnDelete: "false"
