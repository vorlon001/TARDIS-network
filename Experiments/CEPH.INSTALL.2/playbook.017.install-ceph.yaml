---
- hosts: bootstrap
  become: yes
  tasks:
  - name: ansible copy file from remote to local.
    loop: "{{ ceph_rbd }}"
    fetch:
     src: /root/ceph.client.{{ item.name }}.keyring
     dest: /

